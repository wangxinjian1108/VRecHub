interface:
  display_name: "Notify"
  short_description: "Draft or send a concise status notification"
  default_prompt: "Use $notify to draft or send a short status update for this work, including blockers and CI state."

