interface:
  display_name: "Simplify"
  short_description: "Reduce complexity without changing intent"
  default_prompt: "Use $simplify to refactor this code or workflow into a simpler version while preserving its intended behavior."

